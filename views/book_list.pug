extends layout 

block content 
    h1 #{title}
    if(books.length>0)
        ul
            each book in books
                a(href=book.url) 
                    li.bookListItem=book.title
    else 
        p.noItem There's no book